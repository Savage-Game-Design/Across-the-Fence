enablePlayerDiag=false;

hostName = "AN-rubberduck";
password         	= "";			// Password for joining, eg connecting to the server
passwordAdmin       = "-";		// Password to become server admin. When you're in Arma MP and connected to the server, type '#login xyz'
logFile            	= "AN_devServer.log";
verifySignatures = 0;				// Verifies .pbos against .bisign files. Valid values 0 (disabled), 1 (prefer v2 sigs but accept v1 too) and 2 (only v2 sigs are allowed).
equalModRequired = 0;					// kick if data/mods aren't equal
requiredSecureId = 2;					// was used to define type of secureID
allowedVoteCmds[] = {};
//////// !!!! WARNING! TODO: SET TO 0 ON RELEASE !!!!
allowedFilePatching = 2;
filePatchingExceptions[] = {};
motd[] = {
	"Anarchy DEV Server"
};

motdInterval = 5;	          		// Time interval (in seconds) between each message

// JOINING RULES
maxPlayers		= 3;				// Maximum amount of players. Civilians and watchers, beholder, bystanders and so on also count as player.
kickDuplicate 	= 0;					// Each ArmA version has its own ID. If kickDuplicate is set to 1, a player will be kicked 

//localClient[]={127.0.0.1};
battleyeLicense=1;

//////// !!!! WARNING! TODO: SET TO FALSE ON RELEASE OR WHEN USED ON REMOTE SERVER !!!!
loopback = true;					// Adding this option will force server into LAN mode. This will allow multiple local instances of the game to connect to the server for testing purposes. At the same time it will prevent all non-local instances from connecting.

// VOTING
voteMissionPlayers	= 100;        			// Tells the server how many people must connect so that it displays the mission selection screen.
voteThreshold		= 1;     			// 33% or more players need to vote for something, for example an admin or a new map, to become effective

// INGAME SETTINGS
disableVoN = 0;							// If set to 1, Voice over Net will not be available
vonCodecQuality = 30;					// supports range 1-30 //8kHz is 0-10 (narrowband), 16kHz is 11-20 (wideband), 32kHz is 21-30 (ultrawideband)
persistent = 1;							// If 1, missions still run on even after the last player disconnected.
timeStampFormat = "short";				// Set the timestamp format used on each report line in server-side RPT file. Possible values are "none" (default),"short","full".
BattlEye = 1;							// Server to use BattlEye system

disconnectTimeout = 15; 				//  Server wait time before disconnecting client, default 90 seconds, range 5 to 90 seconds. (since Arma 3 update 1.56+)
drawingInMap = 0;						//Enables or disables the ability to place markers and draw lines in map. Default = 1 (since Arma 3 1.63+).

allowedLoadFileExtensions[] = {"txt","hpp"};            // only allow files with ".txt" and ".hpp" extension to be loaded via loadFile command (since Arma 3 1.19.124216)
//allowedPreprocessFileExtensions[] = {"hpp","sqs","sqf","fsm","cpp","paa","txt","xml","inc","ext","sqm","ods","fxy","lip","csv","kb","bik","bikb","html","htm","biedi"}; //only allow files with those extensions to be loaded via preprocessFile/preprocessFileLineNumber commands (since Arma 3 build 1.19.124323)
//allowedHTMLLoadExtensions[] = {"htm","html","xml","txt"}; //only allow files with those extensions to be loaded via HTMLLoad command (since Arma 3 build 1.27.126715)

// SCRIPTING ISSUES
onUserConnected = "";					//
onUserDisconnected = "";				//
doubleIdDetected = "kick (_this select 0)";					//

// SIGNATURE VERIFICATION
onUnsignedData = "";	// unsigned data detected = kick (_this select 0)
onHackedData = 	"";		//tampering of the signature detected = "ban (_this select 0)";
onDifferentData = "";	//onDifferentData = "kick (_this select 0)";

class Missions
{
    class ASC
    {
        template = "ASC_DEV.VR";
        difficulty = "Regular";
    };
};
missionWhitelist[] =
{
	"ASC_DEV.VR"
};
