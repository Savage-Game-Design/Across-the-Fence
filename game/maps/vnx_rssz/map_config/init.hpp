class MapConfig {
    // Sets the maximum visibility a player can have when prone on a surface with this clutter type.
    // Types are found in CfgSurfaceCharacters.
    class Stealth_Prone_GroundClutterVisibilityMax {
        vn_GrassTallClutter = 0.7;
        vn_RainForestClutter = 0.8;
        vn_RiceClutter = 0.6;
    };
};
